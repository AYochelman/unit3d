{
  "name": "reference-studio",
  "version": "1.0.0",
  "private": true,
  "description": "Local reference library and design-brief builder. It has no dependencies of its own: everything, including @anthropic-ai/sdk, comes from the repository root's single npm install.",
  "scripts": {
    "dev": "node scripts/studio.mjs dev",
    "build": "node scripts/studio.mjs build",
    "start": "node scripts/studio.mjs start"
  },
  "dependencies": {}
}
