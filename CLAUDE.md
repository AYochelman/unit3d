# Unit 3D

אתר עברי (RTL) להזמנת הדפסות תלת־מימד בהתאמה אישית. Next.js App Router.

**ענה בעברית.** קוד, שמות משתנים והערות באנגלית; טקסט מוצר בעברית.

## Stack

Next.js 16.2.6 (Turbopack) · React 19 · TypeScript strict · Tailwind CSS 3 · zustand 5

```bash
npm run dev      # http://localhost:3000
npm run build
npx tsc --noEmit # בדיקת הטיפוסים — זו בדיקת האמת בפרויקט הזה
```

`npm run lint` מריץ `eslint .` עם `next/core-web-vitals` + `next/typescript` (flat config, תוקן 3.9). שלושת הבדיקות — `tsc`, `lint`, `build` — עוברות נקי, ושלושתן צריכות לעבור לפני כל קומיט.

## מבנה

```
app/<route>/page.tsx          Server Component — metadata בעברית בלבד
app/<route>/<Name>Client.tsx  "use client" — כל הלוגיקה והאינטראקציה
components/ui/                Btn · Field · Icon · Logo · Pill · SectionHead
components/home/              סקשנים של דף הבית (Hero, AudienceSwitcher, ...)
components/                   Header · Footer · Emblem · ImageCarousel · KeychainPreview
lib/data.ts                   כל תוכן הדמה — יחידות, ביקורות, גלריה, פילמנטים, FAQ
lib/units-hierarchy.ts        עץ 187 יחידות צה"ל (זרוע → חיל → חטיבה → גדוד)
lib/order-store.ts            zustand — עגלת ההזמנה
lib/types.ts                  כל הטיפוסים המשותפים
lib/products.ts               תגים לחיות + בית/משרד (PRODUCTS), מוצרי המעצב (CONFIG_PRODUCTS), משקלי פידג'טים
lib/materials.ts              משפחות פילמנט (PLA/PLA+/Matte/Silk/PETG/TPU/ABS) + מחיר גליל ברירת מחדל
lib/costing.ts                מודל עלות: חומר + מכונה + חשמל + עבודה + פחת AMS → עלות/רווח/מרווח/מחיר מומלץ
lib/admin-store.ts            zustand — PIN, הגדרות עלות, דריסות לכל מוצר, ייצוא/ייבוא JSON (ADMIN_PIN כאן)
lib/design.ts                 המעצב החופשי: פונטים, צורות (shapePath), פלטה, designToSvg, designSummary
components/designer/          DesignCanvas (עורך בסגנון פאוורפוינט: גרירה, שינוי גודל, סיבוב, שכבות, undo) + DesignGroup (רנדור)
components/ProductPreview.tsx תצוגה חיה לכל מוצרי המעצב (מחזיק מפתחות נשאר על KeychainPreview)
components/ProductArt.tsx     איורי SVG למוצרים בלי תמונה (27 סוגים)
components/ProductGrid.tsx    רשת כרטיסי מוצר → /products/[id]
app/admin/                    אזור ניהול (PIN): טבלת עלויות לכל המוצרים, מחירי גלילים, פרמטרים, בדיקת קובצי סמלים, גיבוי
app/pets/ · app/home-office/  הקטגוריות החדשות · app/products/[id] עמוד מוצר (צבע, חומר, AMS, חריטה, כמות, בלוק עלות למנהל)
```

כ-30 מסלולים. העיקריים: `/` `/catalog` `/configurator` `/fidgets` `/fidgets/[id]` `/pets` `/home-office` `/statues` `/trendy` `/products/[id]` `/admin` `/b2b` `/contact` `/gallery` `/livestream` `/reviews` `/tracking` `/upload` `/faq` `/shipping`. הרשימה המלאה: `find app -name page.tsx`.

## המעצב האישי (`/configurator`) — מבנה

שלבים דינמיים לפי המוצר (`CONFIG_PRODUCTS`): מוצר → (דגם) → (צורה) → טקסט/עיצוב → צבע → (גודל) → כמות.
`face: [w, h]` במ"מ הוא הפנים המודפס; קנבס המעצב עובד ב-1 יחידה = 1mm, כך שהעיצוב נשמר בקנה מידה אמיתי.
"עיצוב חופשי" (`mode: "design"`) מחליף את הטקסט המהיר; ההזמנה נושאת `meta.designSvg` (SVG עצמאי) + `meta.designElements`, וטופס יצירת הקשר מציג תמונה ממוזערת.
תוספת מחיר: 15₪ לעיצוב חופשי + 10₪ לכל צבע נוסף (AMS). מחיר כולל חומר לפי הצבע (`materialFromFilamentDesc`); קייסים תמיד TPU.

## פרסום

`.github/workflows/deploy.yml` → GitHub Pages בכל דחיפה ל-main. הוורקפלואו מריץ `tsc` ו-`lint` לפני הבנייה, כך שקוד שבור לא מגיע לאוויר.
`NEXT_PUBLIC_BASE_PATH` מוזרק ע"י הוורקפלואו (Pages מגיש תחת `/<repo>/`); מקומית הוא ריק.

## תמחור: כלל אחד, במקום אחד

`lib/pricing.ts` מחזיק את מדרגת הנחת הכמות (`bulkDiscount` / `lineTotal`). המעצב מצטט דרכה, והסל מחשב אותה מחדש בכל שינוי כמות — לכן שורת סל שומרת מחיר יחידה **לפני** הנחה ב-`meta.baseUnitPrice`, ולא נגזרת מחלוקת הסכום.

תוספת מחיר של חומר היא תמיד ה**דלתא** מעל חומר ברירת המחדל של המוצר (`Math.max(0, mat.priceAdd - default.priceAdd)`), אחרת עמוד המוצר נפתח מעל המחיר שמופיע בקטלוג. הכלל הזה חוזר בשלושה מקומות: עמוד מוצר, עמוד פידג'ט, והמעצב.

## RTL: מלכודות שחזרו בפועל

- **קו מפריד ארוך (–) בין מספרים מתהפך.** "₪50–100" נקרא "100–50" תחת כיוון RTL, כי U+2013 הוא BiDi class ON. להשתמש במקף רגיל, או לעטוף ב-`<bdi dir="ltr">`.
- **`dir="ltr"` על שורה מעורבת הופך את העברית שבה.** לבודד רק את המספר ב-`<bdi>`, לא לעטוף את כל השורה.
- **SVG שמיוצא ונטען דרך `<img>` הוא מסמך מבודד וברירת המחדל שלו LTR.** `designToSvg` מוסיף `direction="rtl"` כדי שמה שהלקוח מזמין יהיה מה שהוא עיצב.

## מובייל: איך בודקים, ומה שבר את הדסקטופ בלי שהרגישו

`npm run audit:mobile` מודד את הבנייה הסטטית בשישה רוחבי טלפון וארבעה של דסקטופ
ומדפיס שלושה מספרים לכל אחד: גובה הדף, האם יש גלילה אופקית, וכמה יעדי מגע קטנים
מ-24px (הסף של WCAG 2.5.8 ברמה AA). להרצה:

```bash
STATIC_EXPORT=1 npm run build
npx http-server out -p 8099        # או כל שרת סטטי
npm run audit:mobile
```

הרוחבים של הדסקטופ נמדדים באותה ריצה ובכוונה: **שינוי מובייל גמור רק כששורות
הדסקטופ לא זזו.** בדיקה עיוור מול צילום מסך לא תופסת הפרשים של 3px.

### המלכודת: `sm:text-*` דורס `leading-*`

מחלקות הגודל של Tailwind קובעות **גם** `font-size` וגם `line-height`. לכן
`text-xs sm:text-sm` שנכתב ליד `leading-relaxed` בלי תחילית גורם ל-`sm:text-sm`
לשבת בתוך media query, לגבור על ה-leading, ולשנות את גובה השורה **בכל רוחב
מ-640 ומעלה** — כלומר בדסקטופ.

זה קרה פעמיים בעבודת המובייל: כרטיס קטגוריה איבד 2.8px, ופסקת ה-hero 2.5px לשורה.
התיקון הוא לחזור ולהצהיר על ה-leading באותה נקודת עצירה (`sm:leading-relaxed`) —
**ולא בזו שאחריה**, כי `md:text-lg` שדורס את ה-leading ב-hero הוא ההתנהגות
הקיימת של הדסקטופ ואסור לתקן אותה.

### `.mobile-cap`

`app/globals.css` — מסתיר את הילד ה-25 ואילך **מתחת ל-640px בלבד**. `/fidgets`
רינדר 144 כרטיסים: 30,728px בטלפון. עם התקרה וכפתור "הצג עוד" זה 7,856px,
והכרטיסים נשארים ב-HTML לזחלנים ולמי שאין לו JS. הדסקטופ מקבל את כל ה-144
כתמיד.

## אזור הניהול (`/admin`)

PIN ב-`lib/admin-store.ts` (ברירת מחדל 1234). המצב נשמר לסשן בלבד (אין localStorage לפי כללי הפרויקט).
כשהמנהל פתוח, עמודי המוצר (פידג'טים, חנות, מעצב) מציגים בלוק עלות ייצור ומרווח. `estimateCost` ב-`lib/costing.ts` הוא מקור האמת היחיד לחישוב.

**לשוניות:** מוצרים · מלאי · גלילים · פרמטרים · סמלי יחידות · גיבוי.

### מלאי (`lib/inventory.ts`)

`StockMap` רושמת רק מה ש**נגמר** (`` `${material}:${color}` `` → `false`), ולכן מפה ריקה = חנות מלאה, וצבע או חומר שיתווספו בעתיד נחשבים זמינים כברירת מחדל.
מוצר שכל הצבעים של החומר שלו נגמרו מאפיר בכל מדף, נכתב עליו "המוצר אינו זמין כרגע", ולחיצה פותחת את `RestockModal` — מייל + צבע מבוקש נכנסים ל-`interest`, והפנייה עצמה נשלחת בוואטסאפ/מייל (אין שרת). לשונית "מלאי" מציגה את רשימת ההמתנה ואת `buyAdvice` — דירוג החומרים לקנייה הבאה לפי `waiting × 3 + blocked`.

### תמחור חי (`lib/live-price.ts`)

מקור אמת יחיד למחיר שהלקוח רואה, לפי הסדר: מחיר ידני מ-`overrides` → `recommendedPrice` מ-`estimateCost` (כש-`pricing.auto` דולק, מעוגל כלפי מעלה ל-`pricing.round`) → מחיר הקטלוג. משמש בכרטיסי המדף, בעמודי מוצר ופידג'ט, במעצב ובקטלוג הסמלים — לכן שינוי מרווח או מחיר גליל מזיז את כל האתר בבת אחת.

### שמירה אמיתית בלי localStorage

לשונית "גיבוי" → **שמירה** מורידה `admin-settings.json` (בלי המיילים של רשימת ההמתנה). מכניסים אותו ל-`public/` ודוחפים ל-GitHub; `components/AdminSettingsBoot.tsx` טוען אותו פעם אחת בעליית האתר, ולכן ההגדרות תקפות **לכל מבקר**, לא רק לבעלים. הקובץ נקרא בזמן build של Next — הוספה שלו בלי build מחדש לא תיקלט.

## מוסכמות

- **RTL** — `dir="rtl"` יושב על `<html>` ב־`app/layout.tsx`. מספרים, יחידות מידה (`mm`, `g`, `h`) ושמות מודלים נשארים LTR.
- **צבע מותג `#089a47`** — ב־Tailwind הוא `brand` **וגם** `flame`. שתי הפלטות זהות לחלוטין; `flame` הוא שריד משם ישן ושתיהן בשימוש. לא לאחד בלי לעבור על כל הקוד.
- **דארק מוד כברירת מחדל.** מוד בהיר עובד דרך `html.light` (ראה `app/globals.css`), לא דרך `dark:`.
- **אסור `localStorage`/`sessionStorage`.** state ב־`useState`/zustand בלבד. חריג יחיד ומתועד: טוקן ה-GitHub של הבעלים ב-`lib/admin-token.ts` — מפתח שלו, במכשיר שלו, כדי שפרסום לאתר יהיה לחיצה אחת.
- **פונטים** — Heebo לטקסט, JetBrains Mono למספרים ולנתונים טכניים, דרך `--font-sans` / `--font-mono`.
- **הכל זמין לעיצוב.** `lib/designable.ts` ממפה כל מוצר לבסיס המתאים במעצב (`designBaseFor`), ובונה קישור `/configurator?product=<base>&from=<id>`. מוצר שהלקוח כותב עליו טקסט (`engraving`, או כותרת שמכילה custom / editable / "עם שם" / "תג שם" / trophy…) מזוהה ע"י `isPersonalizable`, ואז **כרטיס המדף עצמו מוביל למעצב** ולא לעמוד המוצר; "פרטים" נשאר כקישור משני. הזיהוי קורא **כותרות בלבד** — התיאור של מודלים מיובאים הוא בוילרפלייט ("אפשר עם שם או לוגו") שהיה מסמן את כל המדף.
- **כל מסך מוביל לטופס יצירת הקשר.** הקונפיגורציה נוסעת עם המשתמש דרך `useOrderStore` ומוצגת בראש הטופס. זה לב ההמרה של האתר — לא לשבור אותו.

## מה שחייבים לדעת לפני שנוגעים

**קוד המקור שוחזר ממפות בנייה.** התיקייה נמצאה בלי `app/`, `components/` ו־`lib/`, והם חולצו מ־`sourcesContent` שבתוך `.next`. משמעויות:

1. **`lib/types.ts` הוא שחזור**, לא המקור. נכתב מהשימושים בקוד.
2. **הגרסה שרצה היא מ־22.5, וקיימת גרסה מ־28.5** של `lib/data.ts` ו־`app/fidgets/FidgetsClient.tsx` — שמורה ב־`_recovered-newest/`. היא עוברת לדאטהסט `lib/fidgets.generated.ts` (37 פידג'טים) שתלוי בתמונות מקומיות. **לא לעבור אליה עד ש־`public/` תחזור.**
3. **`lib/fidgets.generated.ts` הוא כרגע קוד מת** — אף אחד לא מייבא אותו. זה יתהפך אם עוברים לגרסת 28.5.
4. **`public/` חסרה לגמרי** — 250 תמונות פידג'טים ו־`hero-loop.mp4`. לא ניתנת לשחזור ממפות קוד. ה־`sourceUrl` של כל פריט רשום ב־`lib/fidgets.generated.ts`.
5. **`.next-old/` היא הארכיון שממנו שוחזר הכל. לא למחוק** עד ש־`public/` תחזור.
6. **יש `.git` מאז 3.9** (קומיט ראשון = מצב ההעברה, קומיט שני = תיקוני lint/config). `_recovered-newest/`, `.next-old/` ו־`*.zip` מוחרגים ב־`.gitignore` וב־`tsconfig.json`.

`HANDOFF.md` מכיל את הסיפור המלא ואת רשימת הממצאים הפתוחים.
`docs/emblems-needed.md` — 100 שמות הקבצים ש-`/catalog` מחפש תחת `public/emblems/` (גם בלשונית "סמלי יחידות" ב-/admin, עם בדיקה חיה).
`בריף-לקלוד-דיזיין.md` הוא מסמך העיצוב המקורי — קהלי יעד, פלטה, וכל דרישה לכל עמוד.

## העברה למחשב חדש

הכל בגיט (`main` = מקור האמת, `public/` קיימת). מה ש**לא** בגיט וצריך להקים מחדש: `node_modules`, פרופיל הדפדפן של מייקרוורלד (`scripts\mw-login.bat`), המשימה המתוזמנת של `sync-daily.bat`, טוקן ה-GitHub של האדמין (localStorage, לכל מכשיר), ו-`agent/config.json` אם הסוכן לא על Pi. סדר ההקמה המלא: HANDOFF.md סעיף 21. שאלת `D:\Claude Projects\Unit 3D` מהשחזור המקורי כבר לא רלוונטית.


## איך כל מדף מסודר

`lib/ranking.ts` מחזיק את הניקוד, ו-`lib/signals.generated.ts` את המספרים שמזינים
אותו. חמישה פרמטרים, לכל אחד מספר אמיתי מאחוריו:

| פרמטר | המספר | משקל |
|---|---|---|
| סבירות שיקנו | `saves` (כמה שמרו) + `prints` (כמה הדפיסו) + תגובות | 1.4 |
| כמות הורדות | `downloads` | 1.0 |
| טרנדיות עכשווית | הורדות לחודש מאז `published` | 0.8 |
| נראות התמונות | גודל הגלריה (בלי תמונת השער) | 0.6 |
| רלוונטיות לעמודה | התגיות מול `SHELF_WORDS` | 0.5 |

בונוס 0.3 ל-`isStaffPicked`. כל איבר הוא `log10` — המספרים נעים בין 382 ל-105,957,
ובסקאלה ליניארית שום איבר אחר לא מזיז כלום.

**רלוונטיות היא בונוס, לא קנס.** מודל שהתגיות שלו לא תואמות למדף פשוט לא מקבל את
ה-0.5; הוא לא יורד. רשימות המילים רחוקות מלהיות שלמות, והורדה בגללן מסתירה מוצר טוב
בגלל פער אוצר מילים.

**`npm run fetch:signals` הוא מה שממלא את זה** (רץ ב-`sync-daily.bat` עם `--all`, כי
המספרים זזים). בלעדיו הקובץ ריק, כל מודל מקבל ניקוד לפי הורדות בלבד, והמדפים
נשארים בדיוק בסדר שהיה — זה מצב עובד, לא שבור.

`PINNED` ב-`lib/listing.ts` עוקף את הכל במיון ברירת המחדל: מיסטרי בוקס וכרטיס
הביקור ראשונים, כי אין להם מספרים ובלי זה הם היו בתחתית.

## עמודות החנות (v2.6)

| נתיב | מקור הנתונים | הערות |
|------|--------------|-------|
| `/trendy` | `lib/trending.ts` | מדף מעורב שנבנה מהשאר |
| `/catalog` | `lib/data.ts` UNITS | סמלי יחידות |
| `/fidgets` | `FIDGETS` (data.ts + ייבוא) | טאבים פלקסי/פידג'ט לפי `lib/fidget-kind.ts` |
| `/pets` | `productsByCategory("pets")` | |
| `/statues` | `productsByCategory("statues")` | חדש |
| `/home-office` | `productsByCategory("home","office")` | |
| `/configurator` | `CONFIG_PRODUCTS` | מעצב חופשי |

מוצר חדש נכנס ל-`lib/products.ts` עם `hours` ו-`grams` — בלעדיהם פאנל העלות
למנהל לא יידע לחשב. ציור חדש נוסף ל-`ProductArt.tsx` ול-`ProductArtId`.

`npm run import:makerworld` מוסיף מודלים מיובאים לאותן עמודות בלי שינוי UI.
ראה HANDOFF.md סעיף 15 — כולל אזהרת הרישוי (NC לא נמכר) והמסלול דרך הדפדפן.


## ייבוא ממייקרוורלד — הדרך שעובדת

דפי HTML של makerworld.com חסומים ב-Cloudflare (403). ה-API לא:
`https://makerworld.com/api/v1/design-service/design/<id>` מחזיר 200 לבקשה
רגילה, עם רישיון, שם מעצב, משקל בגרמים, זמן הדפסה בשניות, מספר צבעים, תגיות
וקטגוריות. `scripts/import-makerworld.mjs` בנוי סביב זה, עם מטמון ב-
`data/makerworld-details.tsv` ומצב `--offline`.

**מ-20.9: ה-API מחזיר 403 ל-node ו-200 לדפדפן.** נבדק זה מול זה באותה דקה
ועל אותם ids. מה שזה **לא**: לא הכתובת (דפדפן בדאטהסנטר מקבל 200), לא הגבלת
קצב (שבע בקשות ברצף במרווח 1.2 שניות — כולן נענו), לא ה-user-agent (דפדפן
ששולח בדיוק את המחרוזת `node` עדיין מקבל 200), ולא עוגייה. נשאר איך שהחיבור
עצמו נראה, ואת זה שום כותרת לא משנה.

לכן `getJson` פותח דפדפן **רק אחרי שבקשה רגילה נדחתה**, ומנווט ל-URL (לא
`fetch` מתוך הדף — זה cross-origin; ולא `page.request` — זה לא מחסנית הרשת
של הדפדפן). הדפדפן משותף לכל הקריאות שאחריו, ובלי פרופיל: ה-API לא דורש
התחברות, ודפדפן שני על הפרופיל המחובר הוא בדיוק מה שתופס אותו
("Opening in existing browser session"). `MAKERWORLD_NO_BROWSER=1` מכבה.

**הדפדפן נסגר לבד אחרי 20 שניות בלי שימוש.** דפדפן פתוח הוא תהליך חי ו-node
לא ייצא כל עוד הוא רץ, ולכן סקריפט ששכח לסגור פשוט **נתקע** — בשקט, ונראה
בדיוק כמו ריצה איטית. זה מה שקרה לריצה הלילית: שישה מהסקריפטים שקוראים את
ה-API לא קראו ל-`closeBrowser`, והראשון שפתח דפדפן תקע את כל השרשרת.
`closeBrowser()` עדיין קיים וחוסך את 20 השניות, אבל הוא כבר לא תנאי ליציאה —
כי "לזכור בכל סקריפט" זה כלל שמחזיק עד שנכתב הסקריפט השביעי.

יש גם מגירת עוגיות ב-`getJson` (Cloudflare מחזיר `set-cookie: __cf_bm` ומצפה
לקבל אותה חזרה, ול-`fetch` של node אין מגירה). היא נכונה בפני עצמה אבל **היא
לא מה שפתר את ה-403** — נוסתה, ולא שינתה כלום.

אין endpoint ציבורי לתוכן אוסף, ולכן את הרשימה עצמה אוספים מהדפדפן דרך
`collect-models.html`. ראה HANDOFF.md סעיף 17.


## כל דבר שהבעלים שולח — נכנס לתור האישור, לא למדף

**כלל קבוע.** כשהבעלים מדביק קישור, אוסף או שם של מודל "להוספה", הוא לא נכנס
לחנות ישירות. הוא נכנס ל-`/admin` ← **"מודלים לאישור"**, והבעלים מחליט שם אם
הוא נמכר בכלל ובאילו עמודות (אפשר יותר מאחת). זה לא זהירות יתר: המחיר, המדף
והרישיון הם החלטות שלו, וייבוא אוטומטי מחליט אותן במקומו.

**בלי חריגים.** היה כאן חריג שאישר לבד מודל שהבעלים שמר לאוסף משלו, בהנחה
שהשמירה היא ה"כן". הוא ביטל אותו במילים מפורשות (18.9): *"כל מודל חדש שנכנס
עובר דרך אישור שלי קודם"*. אין דרך לעקוף את השער — לא אוסף, לא לייק, לא
הרצה אוטומטית. `sync-daily.bat` לא מריץ `--publish`, והדגל נשאר בקוד רק
כאופציה ידנית שאף אחד לא מפעיל.

מה שהאוסף כן תורם זה הקשר, לא החלטה: מודל שבא מאוסף בשם `flexi` או `statue`
מגיע עם המדף שלו כבר פתור, ואזהרות `holdsFor` (רישיון NC, נשק, מותג) מסומנות
עליו. זה חוסך לבעלים עבודה בתור — לא מחליף אותו בו.

החלטה קיימת אף פעם לא נדרסת: מודל שנדחה לא חוזר כ"אושר" רק כי הוא עדיין
יושב באוסף.

```bash
npm run add:candidates -- https://makerworld.com/en/models/2335039-military-dog-tag
npm run add:candidates -- 2335039 714373        # גם מספרי מודל נטו
npm run add:candidates -- https://makerworld.com/en/collections/27248266-buisness
```

- מודל בודד נמשך מה-API ונוסף לראש התור, עם המדף המוצע, הרישיון והמספרים.
- **אוסף** נרשם ב-`scripts/makerworld-sources.json` (אין API ציבורי לאוספים),
  והסריקה הלילית `sync-collections` מושכת אותו דרך הדפדפן.
- **התוסף (`extension/`) כותב למסד, לא לתיקיית ההורדות.** היה `chrome.downloads`
  עם `saveAs:false`, אבל ההגדרה "Ask where to save each file" של כרום דורסת
  אותה — כל סריקה שרצה כשאף אחד לא מול המסך פתחה דיאלוג ומתה שם, וזו הסיבה
  שתור האישור נשאר ריק. ההגדרה היא של הדפדפן ולא של התוסף, ולכן שום קוד לא היה
  מגיע אליה. עכשיו: שורה אחת בטבלה `collected_models` (id=1, upsert — תמיד
  הסריקה האחרונה, בלי תור שמצטבר), ו-`npm run ingest:collected` קורא משם.
  הקובץ בהורדות נשאר כ-fallback לשמירה ידנית מ-`collect-models.html`.
  `data/collected-state.json` (לא בגיט) זוכר איזו סריקה כבר נקלטה.
- נשק ורישיונות NC נופלים כאן ולא מוצעים — הם לא ניתנים למכירה בכל מקרה.
- מודל שכבר בחנות, כבר בתור, או שכבר נדחה — לא מוצע שוב.

אחרי ההרצה: קומיט של `lib/candidates.generated.ts`, והתור מופיע ב-ADMIN.
