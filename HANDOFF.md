# Unit 3D — מסמך העברה

> נכתב ב־3 בספטמבר 2026 בסוף סשן שחזור. המטרה: שסשן חדש (או אדם חדש) יוכל להמשיך בלי לשחזר את ההקשר מאפס.

---

## 1. מה קרה כאן

התיקייה `G:\Unit 3D` הכילה **רק קובצי קונפיגורציה ותיקיית `.next`** — בלי `app/`, `components/`, `lib/`, `public/`, `node_modules` או `.git`. כל קוד המקור נעדר.

מפות הקוד (source maps) שבתוך `.next` מכילות את קוד ה־TSX המקורי בשלמותו, כולל את הנתיב המקורי של הפרויקט: **`D:\Claude Projects\Unit 3D`**. משם שוחזרו 56 קבצים.

**התיקייה ב־D: מעולם לא נבדקה** — לא הצלחנו לחבר אותה לסשן. זו עדיין הפעולה הכי משתלמת שנשארה פתוחה.

---

## 2. מצב נוכחי — עובד

הפרויקט רץ ב־`G:\Unit 3D`:

```
npm install
npm run dev     →  http://localhost:3000
```

- Next.js 16.2.6 (Turbopack), React 19, Tailwind 3, zustand
- 13 עמודים, כולם מחזירים 200 ונטענים בלי שגיאות ריצה
- `tsc --noEmit` עובר נקי
- `npm run build` עובר (29 עמודים סטטיים)

---

## 3. מה שוחזר ואיך

56 קבצים חולצו מ־`sourcesContent` שבמפות הקוד. שני דברים לא היו שם:

**`lib/types.ts`** — נכתב מחדש לגמרי מהשימושים בקוד. קובץ טיפוסים טהור נמחק בקומפילציה ולכן לא נשמר באף מפה. הוא מכיל 20 טיפוסים מיוצאים עם איחודי literal אמיתיים (`EmblemShape`, `FidgetSource`, `OrderSource`, `ShapeId`, `SizeId`). **אם המקור יימצא ב־D: — הוא עדיף על השחזור.**

**`app/globals.css`** — במפות נשמר רק הפלט המהודר של Tailwind (56KB). השחזור הוא כתיבה מחדש של החלק המקורי בלבד: `@tailwind` directives ובלוק `@layer base` עם הרקעים, ה־selection, ה־focus ring והסקרולבר. הערכים עצמם חולצו מתוך ה־CSS המהודר ולכן מדויקים.

`app/reviews/page.tsx` נכתב לפי התבנית של שאר העמודים (המפה שלו הייתה ריקה).

---

## 4. ⚠️ הגרסה שרצה היא לא האחרונה

זו הנקודה הכי חשובה במסמך.

בחילוץ היו כמה גרסאות של אותם קבצים, מבניות שונות. הבחירה נעשתה **לפי הגודל** — וזו הייתה טעות. לפי תאריכי השינוי האמיתיים על הדיסק, שני קבצים בפרויקט הם מגרסת **22.5**, בעוד שקיימת גרסה מ־**28.5**:

| קובץ | מה רץ עכשיו (22.5) | הגרסה החדשה (28.5) |
|---|---|---|
| `lib/data.ts` | 22KB, רשימת `FIDGETS` כתובה בפנים עם thumbnails מרוחקים | 10KB, עושה `export { FIDGETS } from "./fidgets.generated"` |
| `app/fidgets/FidgetsClient.tsx` | 14.9KB, תמונה בודדת לכל פידג'ט | 12.1KB, `ImageCarousel` על מערכי תמונות מקומיים |

**למה לא עברנו לחדשה:** היא מסתמכת על `lib/fidgets.generated.ts` (37 פידג'טים) שמפנה ל־250 תמונות תחת `/public/fidgets/…` — ותיקיית `public/` חסרה. מעבר עכשיו יביא 37 כרטיסים בלי אף תמונה. הגרסה הישנה עובדת כי התמונות שלה חיצוניות (MakerWorld, Cults3D).

שתי הגרסאות שמורות. החדשות יושבות ב־`_recovered-newest/`.

**הערה נוספת:** ב־`FidgetsClient` של 22.5 היו חסרים `import Image from "next/image"` וקבוע `SOURCE_LABEL`, והם הוספו ידנית כדי שהקובץ יתקמפל. בגרסת 28.5 שניהם לא קיימים בכלל — היא נקייה. כלומר הגרסה שרצה מכילה תיקון ידני שלא היה במקור.

---

## 5. מה חסר

**`public/` — 251 קבצים.** 250 תמונות פידג'טים ו־`hero-loop.mp4` (וידאו הרקע בדף הבית). **לא ניתן לשחזור ממפות קוד** — הן שומרות קוד בלבד. המקורות של התמונות רשומים ב־`lib/fidgets.generated.ts` (שדות `sourceUrl`), כך שאפשר למשוך אותן מחדש אם ה־D: לא יימצא.

---

## 6. שינויים שנעשו ואינם חלק מהמקור המקורי

**`.vscode/launch.json`** — נוצר חדש. ה־`launch.json` המקורי ישב ב־`.claude/` (מקום ש־VS Code לא קורא), עם `version: "0.0.1"` וללא `type`/`request` — הוא לא היה נטען.

**`.next` → `.next-old`** — הקאש הישן נוצר בגרסת Next אחרת והפיל את השרת (`Failed to open database / invalid digit found in string`). שינוי שם פתר. **אל תמחק את `.next-old`** עד ש־`public/` תחזור — זה הארכיון שממנו שוחזר הכל.

---

## 7. ממצאים פתוחים מהסקירה

לפי חומרה, כולם עדיין לא טופלו:

- **אין `.git`.** יש `.gitignore` בלי ריפו. זה מה שאפשר לתקלה הזו לקרות מלכתחילה. `git init` + ריפו פרטי.
- **`npm run lint` לא עושה כלום.** `eslint.config.mjs` מכיל רק בלוק `ignores` בלי חוקים, והסקריפט מריץ `next lint` שהוסר ב־Next 16. להחליף ל־`eslint .` עם `next/core-web-vitals` ב־flat config.
- **`next.config.ts` — `remotePatterns` רחב מדי.** `**.amazonaws.com`, `**.cloudfront.net`, `**.akamaized.net` הופכים את מייעל התמונות לפרוקסי פתוח. לצמצם ל־hostים בשימוש בפועל.
- **`next-env.d.ts` נערך ידנית** (נוספה שורת `import "./.next/dev/types/routes.d.ts"`) למרות ההערה בקובץ, והוא ב־`.gitignore` — כלומר השורה תיעלם בשכפול נקי. להסיר.
- **כפילות בפלטת Tailwind:** `flame` ו־`brand` הן שתי פלטות זהות לחלוטין. `flame` הוא שריד משם צבע ישן. שתיהן בשימוש בקוד במקביל.
- **`#F5F5F7`** (צבע הטקסט הראשי לפי הבריף) לא קיים בסקאלת `ink` ב־`tailwind.config.ts`. הכי קרוב הוא `ink.50 = #FAFAFA`.
- **Next 16 יוצר `AGENTS.md` ו־`CLAUDE.md` בכל `npm run dev`** ועלול לדרוס את ה־`AGENTS.md` הקיים. לכבות עם `agentRules: false` ב־`next.config.ts`.
- **במעצב, "המשך" ו"הקודם" נראים הפוכים** ביחס לכיוון ה־RTL. לא נבדק לעומק.

---

## 8. הצעד הבא

1. **לבדוק אם `D:\Claude Projects\Unit 3D` קיימת.** אם כן — שם נמצאים `public/`, `lib/types.ts` המקורי, והגרסה האחרונה האמיתית. כל השאר במסמך הזה הופך למיותר.
2. אם היא נעלמה — להחליט: להישאר על גרסת 22.5 שעובדת, או למשוך מחדש 250 תמונות ולעבור ל־28.5.
3. `git init` לפני כל שינוי נוסף.

---

## 9. הקשר לבריף

`בריף-לקלוד-דיזיין.md` בשורש הפרויקט הוא מסמך העיצוב המלא (319 שורות): שלושה קהלי יעד שווי־משקל (פרטיים, חיילים, עסקים), צבע מותג `#089a47`, RTL, דארק מוד כברירת מחדל. הקוד המשוחזר תואם לו כמעט אחד לאחד — כל 13 העמודים שהבריף מבקש קיימים ובנויים.

---

## 10. עדכון — סשן המשך, 3 בספטמבר 2026 (מחשב חדש)

הפרויקט הועבר כ־ZIP לסביבה חדשה ו**כל שלוש הבדיקות עוברות נקי**: `npx tsc --noEmit`, `npm run lint`, `npm run build` (29 עמודים).

מה נעשה:

- **`git init`** + שני קומיטים: (1) מצב ההעברה כפי שהתקבל, (2) התיקונים שלהלן. `_recovered-newest/`, `.next-old/` ו־`*.zip` הוחרגו ב־`.gitignore`.
- **`tsconfig.json`** — `_recovered-newest` נוסף ל־`exclude`. בלי זה `tsc` ו־`next build` נופלים על `./types` שלא קיים בתיקייה ההיא.
- **lint עובד** — `eslint.config.mjs` הוחלף ל־flat config עם `eslint-config-next/core-web-vitals` + `/typescript`, והסקריפט הוא `eslint .`. `ignoreRestSiblings` הופעל בגלל תבנית הסרת ה־props ב־`Btn.tsx`.
- **7 שגיאות lint תוקנו** (כללי React 19 חדשים): `Math.random` בזמן render ב־`B2BClient`/`ContactClient` עבר ל־state שנקבע בעת שליחת הטופס; `setState` בתוך `useEffect` ב־`GalleryClient`/`ImageCarousel` הוחלף בתבנית "derived state during render"; `ThemeToggle` קורא את `html.light` דרך `useSyncExternalStore` עם `MutationObserver`; גרש לא־מוסתר ב־`GalleryClient`; ייבואים לא בשימוש הוסרו.
- **`next.config.ts`** — `remotePatterns` צומצם ל־10 ה־hosts שמופיעים בפועל בנתונים. ה־wildcards של amazonaws/cloudfront/akamaized הוסרו.

מה נשאר פתוח מסעיף 7:

- `agentRules` — האופציה לא קיימת ב־Next 16.2.6 (חיפוש ב־`node_modules/next/dist` לא מוצא אותה). `npm run build` כאן לא דרס את `AGENTS.md`/`CLAUDE.md`. להשאיר במעקב.
- `next-env.d.ts` — Next 16 כותב את שורת ה־`import` בעצמו בכל build/dev, והקובץ ב־`.gitignore`. אין מה לתקן.
- כפילות `flame`/`brand`, `#F5F5F7`, וכיוון כפתורי "המשך"/"הקודם" במעצב — לא נגעתי.
- **`public/` עדיין חסרה** ו־`D:\Claude Projects\Unit 3D` עדיין לא נבדקה. הגרסה שרצה נשארה 22.5.

---

## 11. סשן פיצ'רים — 3 בספטמבר 2026 (המשך)

נבנה על גבי גרסת 22.5 (הרצה). כל השלושה עוברים נקי: `tsc`, `lint`, `build` (53 עמודים סטטיים).

**מה נוסף:**

- **עמוד מוצר פנימי לפידג'טים.** לחיצה על כרטיס ב-`/fidgets` פותחת את `/fidgets/[id]` (לא את אתר המקור). בעמוד: צבע, **חומר** (חדש), AMS, כמות, ובלוק עלות למנהל. הקרדיט ליוצר נשאר קישור חיצוני קטן (חובת רישיון CC-BY).
- **שתי קטגוריות חדשות:** `/pets` (8 תגים לחיות) ו-`/home-office` (12 מוצרים לבית ולמשרד, עם סינון). עמוד מוצר משותף `/products/[id]` עם חריטה, אפשרויות, חומר, צבע, AMS. איורי SVG (`ProductArt`) עד שיש צילומים. כרטיסים בדף הבית + ניווט עודכנו.
- **אזור ניהול `/admin`** (PIN): טבלת כל המוצרים (פידג'טים, חנות, מעצב) עם זמן, גרם, עלות חומר/מכונה/חשמל/עבודה, מחיר, רווח, מרווח ומחיר מומלץ; עריכת גרם/זמן/מחיר לכל שורה; מחירי גלילים לכל חומר; פרמטרים (₪/שעה מכונה, וואט, קוט"ש, עבודה, פחת AMS, יעד מרווח); בדיקת קובצי סמלים; ייצוא/ייבוא JSON.
- **המעצב האישי הורחב ל-10 מוצרים:** מחזיק מפתחות, דיסקית, קייס לטלפון (14 דגמים), קייס למצית (3), תג למזוודה, שלט שם (3 גדלים), תחתית לכוס, וו לקיר, קליפ לכבלים, סימנייה. שלבים דינמיים, תצוגה חיה בפרופורציות אמיתיות (`ProductPreview`).
- **עיצוב חופשי (PowerPoint-style):** `DesignCanvas` — טקסט ב-8 פונטים עבריים (Heebo, Rubik, Assistant, Secular One, Frank Ruhl Libre, Suez One, Karantina, JetBrains Mono, נטענים ב-`app/layout.tsx`), 10 צורות, פלטת פילמנטים + צבע חופשי, גרירה, ידית שינוי גודל, סיבוב, שכבות, שכפול, undo/redo, מקלדת (חצים/Delete). היצוא הוא SVG במ"מ שנוסע עם ההזמנה לטופס.

**החלטות שכדאי לדעת:**

- אין localStorage (כלל מהבריף). לכן הגדרות המנהל הן לסשן, עם ייצוא/ייבוא. שמירה אמיתית = שרת/Supabase.
- הערכות גרם/זמן למוצרים החדשים הן אומדנים סבירים, לא מדידות סלייסר. עדכן ב-/admin או ישירות ב-`lib/products.ts`.
- `public/emblems/` עדיין חסרה — הקוד מוכן, רק הקבצים חסרים. הרשימה ב-`docs/emblems-needed.md`.
- מחירי הגלילים ב-`lib/materials.ts` הם מחירי שוק ישראלי טיפוסיים ל-2026; יש לעדכן לפי הספק בפועל.

**עדיין פתוח:** צילומים אמיתיים למוצרים החדשים; תמונות ל-`_recovered-newest` (גרסת 28.5 של הפידג'טים) — עדיין לא הוחלה; שמירת הגדרות מנהל בשרת; טופס יצירת קשר לא שולח לשום מקום (אין backend).

---

## 12. סשן v2.2 — 4 בספטמבר 2026

- **טרנדי כרגע** (`/trendy`, `lib/trending.ts`): מדף מעורב שמפנה לפידג'טים, מוצרי החנות ומוצרי המעצב (`/configurator?product=<id>`). כרטיס "אני מזמין בשבילי" בדף הבית מוביל לשם.
- **סינון ומיון בכל הרשימות** (`lib/listing.ts` + `components/ProductToolbar.tsx`): פופולרי / הכי מוזמן / דירוג / מחיר יורד-עולה / חדש; סינון לפי מספר צבעים (1 / 2 / 3–4 AMS) ולפי טווח מחיר. הנתונים (דירוג, הזמנות) הם מונים לדוגמה ב-`PRODUCT_STATS` / `FIDGET_STATS` בתוך `lib/products.ts` עד שיהיה backend.
- **סל**: שורה נושאת `qty` + `unitPrice`; `setQty` ב-`lib/order-store.ts`; בטופס יש +/- ומחיקה.
- **דף הבית**: לוגו U3D מונפש (`components/home/HeroLogo.tsx`, קיפריימים `u3d-*` ב-globals.css), ביקורות בשני פסים נעים (`ReviewsRow`), מדף "מה יוצא מהמדפסת" (`ProductShowcase`), 8 כרטיסי קטגוריה.
- **לוגו חדש** ב-`components/ui/Logo.tsx` (סימן U3D + WORDMARK).
- **פידג'טים**: אין יותר קישורים חיצוניים בכרטיסים; קרדיט + קישור לקובץ המקור נמצאים בתחתית עמוד המוצר (דרישת CC-BY).
- **CSS שאבד בשחזור הוחזר**: `.btn-primary` (הכפתורים הירוקים!), `.printer-grid`, `.stripes`, `.live-dot`, `.marquee-track`, `.spin-y`, `.hero-*` + הקיפריימים `marquee`/`livepulse`/`slowspin` כ-CSS רגיל (Tailwind פולט קיפריימים מה-theme רק לשימוש ב-`animate-*`).
- **ניווט**: טרנדי, לחיות, בית ומשרד בתפריט הראשי; העלאת קובץ/לייב עברו לתפריט המשני ולפוטר; גלגל שיניים בכותרת → `/admin`; חותמת גרסה בפוטר.
- **סמלי יחידות**: החיפוש רץ דרך WebSearch בלבד (הרשת מהסביבה הזו חסומה ל-wikimedia). התוצאה: `scripts/fetch-emblems.mjs` + `docs/emblems-sources.md` (ראה שם את מצב האימות לכל יחידה). ההורדה עצמה רצה על המחשב של המפעיל.

---

## 13. סשן v2.3 — סקירת קוד אדוורסרית, 4 בספטמבר 2026

הורצה סקירה בחמישה מבטים (נכונות ריצה, ייצוא סטטי/הידרציה, RTL ונגישות, המעצב, נתונים ותמחור) על כל השינויים מ-v2.1 והלאה, כאשר כל ממצא נבדק בנפרד ע"י שני בודקים שתפקידם היה להפריך אותו. 37 ממצאים ייחודיים, **31 אושרו ותוקנו**.

### הבאגים החמורים שנמצאו

| # | הבאג | למה זה היה חשוב |
|---|---|---|
| 1 | `Btn` רינדר `<a>` רגיל גם לקישורים פנימיים | כל CTA היה טעינת עמוד מלאה שמאפסת את הסל (שאין לו persistence בכוונה). כולל הכפתור בדף הבית שמוביל לטופס ההזמנה עצמו. |
| 2 | תוספת מחיר החומר נגבתה גם על חומר ברירת המחדל של המוצר | כל עמוד מוצר נפתח במחיר גבוה מזה שמופיע בקטלוג. |
| 3 | הנחת הכמות נשמרה בתוך המחיר | עריכת כמות בסל מ-5 ל-4 השאירה 10% הנחה; מ-1 ל-5 לא נתנה אותה. |
| 4 | `?product=` קיבל מפתחות תורשתיים (`constructor`) | קריסה מקישור עמוק. |
| 5 | `designColorCount` התעלם מקווי מתאר | עיצוב בשני פילמנטים תומחר כצבע אחד. |
| 6 | קווים מפרידים (en dash) בין מספרים | "₪50-100" נקרא "100-50" תחת כיוון RTL. תוקן בכל האתר. |
| 7 | ה-SVG המיוצא להזמנה בלי כיוון וללא חיתוך לפנים המוצר | הלקוח היה מקבל עיצוב שונה ממה שראה. |
| 8 | `preventDefault` ב-pointerdown השאיר מיקוד בשדה הטקסט | Delete וחצים לא הגיעו לקנבס. |
| 9 | חישוב שינוי גודל התעלם מסיבוב | גרירת הידית הגדילה לכיוון הלא נכון. |

### מה שנבנה כתוצאה

- **`lib/pricing.ts`** — כלל הנחת הכמות במקום אחד, משותף למעצב ולסל.
- **`facePath` ב-`lib/design.ts`** — הגדרה אחת של צללית המוצר לקנבס, לתצוגה המקדימה וליצוא, כך שכל השלושה מסכימים.
- שורת סל נושאת מחיר יחידה **לפני** הנחה (`meta.baseUnitPrice`), והסל מחשב את המדרגה מחדש בכל שינוי כמות.

### עדיין פתוח

הטופס לא שולח לשום מקום (אין backend), הגדרות המנהל נשמרות לסשן בלבד, ואין צילומים אמיתיים למוצרים החדשים.

---

## 14. פרסום אוטומטי (v2.5)

`.github/workflows/deploy.yml` בונה ומפרסם ל-GitHub Pages בכל דחיפה ל-main.
הוא מריץ קודם `tsc --noEmit` ו-`npm run lint`; אם אחד מהם נכשל, הפרסום נעצר ולא עולה לאוויר.

`next.config.ts` קורא `NEXT_PUBLIC_BASE_PATH`. Pages מגיש ריפו פרויקט תחת `/<repo>/`, והוורקפלואו מזריק את הקידומת הזו. מקומית המשתנה ריק והכל נשאר בשורש, כך שאותו קוד עובד בשני המקומות.

**מה עדיין ידני, פעם אחת:** יצירת הריפו ומתן גישה. אין לי הרשאה ליצור ריפו (`create_repository` מחזיר 403 — ה-GitHub App של הסשן מוגבל לריפו של MOCA), וכל ספקי האחסון האחרים (Netlify, Vercel, Cloudflare) חסומים מהסביבה הזו. GitHub API הוא היעד היחיד שנגיש.
